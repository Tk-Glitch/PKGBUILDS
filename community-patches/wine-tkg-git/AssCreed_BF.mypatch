diff --git a/dlls/user32/winpos.c b/dlls/user32/winpos.c
index 4b39d0aa393..6a283d6e5ae 100644
--- a/dlls/user32/winpos.c
+++ b/dlls/user32/winpos.c
@@ -1266,6 +1266,9 @@ BOOL WINAPI ShowWindow( HWND hwnd, INT cmd )
     if ((cmd == SW_HIDE) && !(GetWindowLongW( hwnd, GWL_STYLE ) & WS_VISIBLE))
         return FALSE;
 
+    if ((cmd == SW_SHOW) && (GetWindowLongW( hwnd, GWL_STYLE ) & WS_VISIBLE))
+        return TRUE;
+
     return SendMessageW( hwnd, WM_WINE_SHOWWINDOW, cmd, 0 );
 }
 
